sudo: false

language: go

go:
  - "1.9.x"
  - "1.10.x"
  - "1.11.x"
  - master

install:
  - go get -t -v ./...

script:
  - go test -v ./... -race -coverprofile=coverage.txt -covermode=atomic

matrix:
  allow_failures:
    - go: master
  fast_finish: true